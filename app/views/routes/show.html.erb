<%= render 'layouts/navbar' %>
<div class="well">
  <% @routelist.each do |k,v| %>
    <% v.each do |key, value| %>
      <h2><%= link_to "#{key['display_name'][5..40]}", stops_path(:route_id => "#{key['id']}") %></h2>
     <% end %>
  <% end %>
</div>
